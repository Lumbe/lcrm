Rails.application.routes.draw do
  # %w(accounts campaigns contacts leads opportunities tasks).each do |controller|
  #  get "/#{controller}/auth_user_before_filter" => "#{controller}#auth_user_before_filter", :as => "#{controller}_auth_user_before_filter"
  # end
end
